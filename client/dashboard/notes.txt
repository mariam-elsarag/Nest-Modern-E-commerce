🔑 Best practice (combined approach)

In Settings:

Add a toggle: Enable Tax.

Add a default tax rate (e.g., 15%).

In Product Create/Edit:

Show a "Use default tax" toggle (default: true).

If toggled off → show an input for a custom tax rate (or "No tax").

----------------
remaning:
- Contact us ✅
- websitce contact ✅
- Notification ✅
- Settings ✅
- menue component
- refund order
- order details
_________________
backend:
1- need endpoint to get default rate

2- notification type(   | "order"
    | "review"
    | "contact"
    | "outOfStock"
    | "achieveGoal"
    | "missedGoal") (order,review,hit goal for orders, contact,out of stock products)
3- invoice ui
4

empty for pages has details